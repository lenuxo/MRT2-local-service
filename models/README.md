# Local model data

Run `../scripts/download_models.sh mrt2_small` or pass both model names. The official Magenta CLI writes shared assets to `resources/` and exported MLX models to `models/` below this directory.

Downloaded weights are intentionally ignored by Git.
