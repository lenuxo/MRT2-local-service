"""CLI、OpenAPI 和协议模型共用的简明参数说明。"""

DURATION = "生成时长（秒），范围 (0, 300]；省略时为 10 秒"
TEMPERATURE = "采样随机度；越高变化越大，越低越保守；必须大于 0，省略或 null 使用服务默认值"
TOP_K = "每一步只从概率最高的 K 个候选中采样；越小选择越集中；必须大于等于 1，省略或 null 使用服务默认值"
CFG_MUSICCOCA = "文本/参考音频风格的遵循强度；通常保留默认 3.0；官方会把数值截断到 [-1, 7] 并按 0.2 量化"
CFG_NOTES = "模型遵循 notes 音高与起止时间的强度；值越高越贴合指定音符，默认 1.0"
CFG_DRUMS = "模型遵循 drums 鼓点触发时间的强度；值越高越贴合指定节奏，默认 1.0"
SEED = "文本 mapper 的随机种子；仅在 use_mapper=true 且存在文本条件时生效，不保证整段音频可复现"
USE_MAPPER = "是否把文本 embedding 映射到音频风格空间；仅影响文本条件，建议保持启用"
POOL_ACROSS_TIME = "是否把参考音频各时间片的 embedding 求平均为一个整体风格；文本与音频混合时必须启用"
TEXT_WEIGHT = "文本风格在混合条件中的相对权重；非负，并会与音频权重一起归一化"
AUDIO_WEIGHT = "参考音频风格在混合条件中的相对权重；非负，并会与文本权重一起归一化"
PROMPT_COMPONENTS = "高级文本风格混合；各片段分别编码后按 weight 合成为整体文本条件；与 prompt 互斥，最多 8 项、单项最多 1000 字符、总计最多 4000 字符"
PROMPT_COMPONENT_TEXT = "独立编码的音乐风格描述；官方编码器最多使用前 127 个文本 token"
PROMPT_COMPONENT_WEIGHT = "该文本片段相对其他片段的影响权重；范围 0～1000000，服务会自动归一化"
CHUNK_FRAMES = "每个 PCM 应用分片包含的 40 ms 模型帧数，范围 1～25；默认 5（约 200 ms）"
WARMUP_STEPS = "模型启动时用于预热 MLX 的空推理步数；只影响首次使用延迟和启动耗时，不改变生成内容"
